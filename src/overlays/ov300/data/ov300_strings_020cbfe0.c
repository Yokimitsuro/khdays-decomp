/* ov300 .data strings, 0x020cbfe0-0x020cc000.
 *
 * 1 symbols in one contiguous run. Each array is sized as the original is, so
 * the literal supplies the text and the declared length pads the rest with NUL.
 */

char data_ov300_020cbfe0[32] = "Ms/%02x.p";
