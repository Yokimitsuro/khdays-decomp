/* ov045 .rodata strings, 0x020b4a7c-0x020b4aac.
 *
 * 3 symbols in one contiguous run. Each array is sized as the original is, so
 * the literal supplies the text and the declared length pads the rest with NUL.
 */

const char data_ov045_020b4a7c[16] = "zexion_R";

const char data_ov045_020b4a8c[16] = "zexion_tg";

const char data_ov045_020b4a9c[16] = "Bip01";
