/* ov010 .data strings, 0x0204cfb4-0x0204cfe0.
 *
 * 2 symbols in one contiguous run. Each array is sized as the original is, so
 * the literal supplies the text and the declared length pads the rest with NUL.
 */

char data_ov010_0204cfb4[20] = "/UI/sys/sys_&.s.z";

char data_ov010_0204cfc8[24] = "text/font_eu_10.nftr";
