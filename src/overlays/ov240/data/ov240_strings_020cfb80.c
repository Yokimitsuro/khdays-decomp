/* ov240 .data strings, 0x020cfb80-0x020cfbc0.
 *
 * 3 symbols in one contiguous run. Each array is sized as the original is, so
 * the literal supplies the text and the declared length pads the rest with NUL.
 */

char data_ov240_020cfb80[12] = "Ms/%02x.p";

char data_ov240_020cfb8c[16] = "Ms/NBShare.p";

char data_ov240_020cfb9c[36] = "move";
