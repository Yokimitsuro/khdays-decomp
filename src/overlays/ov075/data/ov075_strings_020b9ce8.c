/* ov075 .rodata strings, 0x020b9ce8-0x020b9d18.
 *
 * 3 symbols in one contiguous run. Each array is sized as the original is, so
 * the literal supplies the text and the declared length pads the rest with NUL.
 */

const char data_ov075_020b9ce8[16] = "demyx_tg";

const char data_ov075_020b9cf8[16] = "demyx_R";

const char data_ov075_020b9d08[16] = "Bip01";
